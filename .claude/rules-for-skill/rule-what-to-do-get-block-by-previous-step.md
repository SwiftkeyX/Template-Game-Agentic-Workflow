# Rule: Blocking Issue

When you discover a blocking issue rooted in a prior pipeline stage — a design flaw, an architectural mismatch, or a systemic bug that cannot be safely patched within the current skill scope — call `/regress` instead of patching symptoms.

Do not work around it. Surface it.
