# Rule: PIPELINE.md Tick

Tick a PIPELINE.md item only after its exit condition is fully met — verified by testing or user confirmation, never by code inspection alone.

Change `- [ ] <item>` to `- [x] <item>`. If all items in a tier are now checked, tick that tier's milestone line too.

Never tick before the work is verified.
